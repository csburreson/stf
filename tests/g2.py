import stf
import re as barf

def run_test(test, session, foo=None, bar=None, czar="King"):
    barf.compile

stf.register(run=run_test, test_name='g2', version='1.0')

if __name__ == '__main__':
    stf.run()
