import stf
import re as reFoo

def run_test(test, session, foo=None, bar=None, czar="King"):
    reFoo.compile

stf.register(run=run_test, test_name='g1', version='1.0')

if __name__ == '__main__':
    stf.run()
